# AeroBag Predictor v10.0 - Журнал состояния и архитектуры

## Информация о проекте
- Название: AeroBag Predictor v10.0 (MySQL Edition)
- Область: Расчёт коммерческой загрузки и прогнозирование веса багажа в авиации
- Стек технологий: HTML5, CSS3 (тёмная и светлая HUD-тема), Vanilla JavaScript, PHP Backend (api.php), MySQL БД / JSON резервный файл (baggage_db.json)

## Ключевые компоненты и файлы
- `index.html`: Главный интерфейс программы (прогнозирование, предварительный план загрузки, таблицы багажников ULD/BULK, история, окно авторизации администратора).
- `app.js`: Логика приложения на стороне клиента, автозаполнение, алгоритмы расчёта за 30 дней, распределение по ULD/BULK контейнерам, фиксация золотой рамкой, остаточный вес (REST), сохранение истории.
- `style.css`: Оформление интерфейса, авиационная тема HUD, золотое неоновое свечение, переменные тёмной/светлой темы.
- `api.php`: Серверные эндпоинты API для синхронизации с базой данных и получения истории рейсов.
- `db_config.php`: Реквизиты и настройки подключения к базе данных MySQL.
- `baggage_db.json`: Офлайн-набор данных для резервного использования.
- `USER_MANUAL.md`: Подробная инструкция и руководство пользователя для диспетчеров.

## Текущее состояние системы
- Ядро прогнозирования: АКТИВНО (коэффициенты PCS/PAX, Weight/PC, HB/PAX).
- Сетка загрузки контейнеров: АКТИВНА (ULD 1..12, BULK 1..4, автораспределение остатка REST, ручная фиксация веса с золотым свечением).
- Пароль администратора: `NW2026`.
- Источники данных: Файлы Excel (`AVERAGO BAGGEGE WEIGHT TABLE 07_2026.xlsx`, `stats_july.xls`).

## Журнал изменений и действий
- Инициализирован журнал состояния проекта `State.md`.
- Переработано руководство пользователя `USER_MANUAL.md` простым и понятным языком с подробным разбором настроек анализа.
- Расширен Раздел 3 в `USER_MANUAL.md` с глубоким разбором авиационных сценариев, глубины анализа и параметров сезонности.
- Сформирован и отформатирован финальный документ `USER_MANUAL.docx` со стилями и встроенными скриншотами.
- Оптимизирована адаптивность для мобильных устройств в `style.css` (устранён автозум на iOS, крупные области нажатия 40-44px, горизонтальный скролл таблиц, гибкие кнопки шапки).
- Инициализирован Git-репозиторий в папке проекта, создан `.gitignore`, привязан репозиторий `https://github.com/kekc85/aerobag.git` и отправлена первоначальная версия в ветку `main`.
- Установлен регламент работы: Ведение `State.md` на русском языке, автоматическая выгрузка всех изменений (commit & push) на GitHub.
- Обновлена логика вывода предупреждений: при отсутствии данных по дню недели за 60 дней вывод плашки исправлен с сообщением об использовании среднего значения за последние 30 дней.
- Отменена функция экспоненциального весового сглаживания (разного веса свежести рейсов). Теперь все 4 последних рейса учитываются абсолютно одинаково в суммарном среднем.
- Исправлена логика фолбэка периода "День недели (4 рейса)": если за 60 дней до выбранной даты нет рейсов на этот день недели, система производит отсчет 30 дней строго от заданной даты вылета (без привязки к дате последней загруженной базы). Текст плашки обновлён: "Использовано среднее значение за ближайшие 30 дней до вылета".
- Изменена формула подсчёта пассажиров (PAX): младенцы (РМ / INF) исключены из общего числа пассажиров для расчёта средних нормативов коммерческой загрузки. Общее число пассажиров для прогнозирования рассчитывается как `PAX = ВЗ (Взрослые) + РБ (Дети)`.
- Вынесена функция `getEffectivePaxCount` в глобальную область видимости `app.js` для устранения ошибки `ReferenceError`.
- Исправлена верстка информационного подвала `coef-source-info` в `style.css`: устранено наплывание наименования источника данных на метку заголовка за счет использования `flex-wrap` и гибкиx отступов `gap`. Добавлено неоновое выделение значения.
- Добавлено непрерывное сохранение пользовательских значений таблицы "[ СРЕДНИЙ ВЕС БАГАЖА И ДАННЫЕ ЗАГРУЗКИ ]" (LIR / ФИНАЛЬНЫЙ и отсеков BULK) для каждого рейса из истории расчетов. При клике на другой рейс введенные диспетчером данные мгновенно сохраняются и восстанавливаются при обратном переключении.
- Устранена циклическая рекурсия функций автосохранения при стартовой загрузке приложения. Восстановлена моментальная инициализация выпадающих списков аэропортов и рейсов.
- Исправлено заполнение значений выпадающего списка аэропорта вылета (`selectFrom`): опции переведены на сквозной 3-буквенный стандарт ИАТА (AER, KZN, SVO, REN...), благодаря чему при выборе номера рейса автоподбор маршрута мгновенно находит и активирует соответствующие аэропорты вылета и прилета.
- Устранена непарная скобка `SyntaxError` в конце `app.js`. Восстановлена полная работоспособность скрипта, темы, переключателя языков и интерактивности кнопок.
- Исправлено выравнивание шапки таблицы "История расчетов прогнозов" (`.flights-table th`): задан единый паддинг `padding: 10px 12px` и пропорциональные процентные ширины для всех 8 колонок таблицы, исключающие смещение заголовков относительно данных.
- Сформирован и обновлён свежий чистый ZIP-архив продакшн-сборки [`aerobag_build.zip`](file:///e:/ANTIGRAVITY/AVERAGO%20BAGGEGE%20WEIGH/aerobag_build.zip) для разворачивания на хостинге (Beget).
- Введена система точной релизной версий сборок приложения (`v11.3.3`). Константа версии выведена в подвал страницы `index.html` и привязана к автообновлению кэша скриптов `app.js?v=11.3.3`.
- Проведена исчерпывающая оптимизация мобильного вида для смартфонов Android/iOS (релиз `v11.3.3`): зафиксирован гарантированный отступ `padding-left: 24px !important` у всех заголовков панелей от уголка HUD, заданы аккуратные боковые отступы `padding: 14px` внутри карточек и стопроцентное расширение кнопок без обрезки текста справа.
- Таблица "История расчетов прогнозов" (`#predictions-table`) переведена на гарантированную сетку `table-layout: fixed` с объявлением точной группы колонок `<colgroup>`, обеспечивающей миллиметровое совпадение ширины ячеек шапки `<th>` и ячеек данных `<td>`.
- Создана и выгружена на GitHub отдельная экспериментальная ветка `dev` (`git checkout dev`). Ветка `main` зафиксирована как стабильная рабочая версия.
- Реализована **интерактивная подсветка направлений**: при клике на любой дуговой сегмент Круговой диаграммы или строчку легенды выбранный маршрут эффектно подсвечивается с масштабированием толщины кольца (`stroke-width: 18px`) и неоновым свечением, а в центре кольца цифра мгновенно переключается на точный объем вылетов и процент данного маршрута. Повторный клик сбрасывает выделение.
- Выполнен **комплексный редизайн UI по принципам `frontend-design`**:
  * **Hero Result Panel**: Карточка рассчитанного веса багажа выделена как главная визуальная доминанта (`font-size: 2.1rem`, акцентный градиент фона, статус `TOTAL BAGGAGE`).
  * **Daylight Cockpit Console (Светлая тема)**: Усилена контрастность шрифтов (`#0f172a`, `#0284c7`, `#d97706`), добавлены четкие границы карточек (`#e2e8f0`), улучшены выпадающие списки и треки графиков.
  * **Тактильные микро-взаимодействия**: Добавлен отклик при нажатии на кнопки (`:active { transform: scale(0.98); }`), плавный переход вкладок HUD и подсветка строк направлений в дашборде.
  * **Очистка визуального шума**: Смягчена фоновая сетка координат (`opacity: 0.012`), убраны дублирующиеся рамки.
- Восстановлено 4-колоночное горизонтальное расположение полей блока «Распределение пассажиров» (`.pax-grid: grid-template-columns: repeat(4, 1fr)`) в форме ручного добавления рейса.
- Исправлена авторизация и переключение на панель «Администрирование»: устранен сбой в CSS-правиле `.tab-content.hidden`, функция переключения вкладок рефакторизована в `switchTab`, гарантируя немедленный показ вкладки администрирования после успешного ввода пароля `NW2026`.
- Исправлена ошибка `ReferenceError: renderAirportsList is not defined`: функция заменена на `populateAirportDropdowns()`, панель администрирования открывается чисто и без сбоев.
- Разнообразены типы визуализаций в дашборде аналитики:
  * **Панель 1 (Вес на 1 PAX)**: Ранжированные горизонтальные полосы с бейджами ТОП-направлений (`#1`, `#2`, `#3`...).
  * **Панель 2 (Места на 1 PAX)**: Переведена на карточки емкости (Capacity Tiles) со статусами (`Heavy`, `Standard`, `Light`) и бейджами ранга.
  * **Панель 3 (Дни недели)**: Переведена на вертикальную столбчатую диаграмму (Vertical Columns Chart) с неоновыми столбиками, значениями веса над каждым днем и неоновой подсветкой дня с пиковой нагрузкой (`peak-day`).
  * **Панель 4 (Вылеты)**: Круговая диаграмма (Donut Pie Chart) с легендой и интерактивным выбором.
- Добавлены подробные подписи и единицы измерения в диаграмму дней недели: подзаголовок «⚖️ Средний вес багажа на 1 пассажира (кг/PAX)», единицы измерения (`кг` / `kg`) над каждым столбиком и бейдж `MAX` над пиковым днем вылета.
- Устранен вертикальный скроллбар в карточке «Динамика загрузки по дням недели»: контейнер переведен на `overflow: hidden`, высота колонок и подписей оптимизирована, диаграмма идеально и на 100% помещается в карточку дашборда без прокрутки.
- Заголовки панелей дашборда оптимизированы до компактных 1-строчных названий с автоматическим `text-overflow: ellipsis`, исключая некрасивый перенос строк на мобильных.
- Для круговой диаграммы на смартфонах зафиксировано горизонтальное расположение (`flex-direction: row`), при котором кольцо диаграммы (100px) и прокручивающаяся легенда с направлениями и процентами располагаются рядом без выпадения из карточки.
- Восстановлена полоса прокрутки для списков направлений в дашборде (`.chart-bars-list` и `.pcs-tiles-list` с `max-height: 220px; overflow-y: auto`).
- Исправлена мобильная вёрстка вкладки «Прогнозирование»:
  * Колонкам воркспейса задан `grid-template-columns: minmax(0, 1fr)` и `min-width: 0`, исключая распирание формы прогнозирования за правый край экрана.
  * Для таблицы коммерческой загрузки (`.unified-load-wrapper`) включен независимый горизонтальный тач-скролл с красивым скроллбаром (`overflow-x: auto; touch-action: pan-x pan-y`).
- Выполнено успешное слияние ветки `dev` в основную стабильную ветку `main` (`git checkout main && git merge dev`). Все визуальные улучшения, новые графики дашборда, исправления мобильной адаптивности и скроллбаров перенесены в основной релиз.
- Устранена погрешность округления в таблице коммерческой загрузки (BULK): при распределении всех мест багажа по отсекам (`Остаток мест = 0`) последний заполняемый отсек автоматически забирает точный математический остаток целевого веса багажа. В поле «ОСТАТОК» гарантированно фиксируется `0` мест и `0` кг.
- В ветке `experimental` реализован **Этап 1** новой архитектуры прогнозирования:
  * **Smart Waterfall (4-уровневый интеллектуальный каскад)**: 🟢 Уровень 1 (Рейс + День недели) ➔ 🔵 Уровень 2 (Маршрут + День недели) ➔ 🟡 Уровень 3 (Маршрут, все дни) ➔ ⚪ Уровень 4 (Норматив аэропорта).
  * **Адаптивное окно анализа**: окно расширено до 180 дней, глубина выборки увеличена до 20 актуальных рейсов (вместо жесткого лимита 4 рейсов за 60 дней).
  * **Взвешенное отношение**: расчет ведется строго по отношению суммарного багажа к суммарным пассажирам.
  * **Учет неявки пассажиров (No-show 3%)**: добавлен интерактивный переключатель «Факт (100%)» / «Бронь (97% явка)» с динамической подсказкой эффективных PAX.
- В ветке `experimental` реализован **Этап 2** новой архитектуры прогнозирования:
  * **Экспоненциальное сглаживание с памятью ($\alpha = 0.3$)**: хронологическое обучение с весами свежести ($30\%, 21\%, 14.7\%\dots$), надежная защита от случайных выбросов и аномалий.
  * **Многолетняя авто-сезонность**: автоматический расчет помесячных сезонных множителей по 3-летней базе полетов (7 939 рейсов).
  * **Режим «Бронь (97% явка)»**: выставлен по умолчанию с интерактивным живым пересчетом эффективных PAX при вводе.
- Порог надёжности выборки для Уровня 1 (Рейс + День) и Уровня 2 (Маршрут + День) повышен с $\ge 3$ до $\ge 5$ рейсов за 180 дней.
- Полная локализация вкладки «Дашборд аналитики» для английской раскладки:
  * Переведены все заголовки графиков, подзаголовок дашборда, фильтры («ROUTE / AIRPORT», «TIME PERIOD», «All Routes», «All Time»...).
  * Переведены бейджи статуса загрузки («HEAVY», «STANDARD», «LIGHT»), дни недели («MON»-«SUN»), счетчики рейсов, единицы измерения («kg/passenger», «pcs/passenger») и центр круговой диаграммы («FLIGHTS»).
- В ветке `experimental` реализован **Этап 3** новой архитектуры прогнозирования:
  * **Модуль исторического бэктестинга (Backtesting Engine)**: хронологическая симуляция прогнозов на сотнях реальных рейсов без заглядывания в будущее.
  * **Сравнительный анализ алгоритмов**: параллельный расчет Старого метода (4 рейса / 60 дней) и Нового Smart Waterfall ($\alpha=0.3$ + авто-сезонность + 97% явка).
  * **Метрики качества**: MAE по весу и местам, процент попадания в коридор $\pm 10\%$, расчет снижения ошибки (gain %).
  * **Авиационный HUD интерфейс верификации**: модальное окно с выбором периода/лимита, анимированным прогресс-баром и таблицей примеров реальных вылетов.
- Релиз версии **v12.0.80**:
  * Внедрена система автоматического инкремента SemVer (`bump_version.js`, `version.json`, git `pre-commit` hook).
  * В подвале приложения отображается канонический формат версии `AeroBag Predictor v12.0.80 (17.08.2026)`.
  * Слияние ветки `experimental` с основной веткой `main`.
- Обновлено Руководство пользователя (**USER_MANUAL.md** и **USER_MANUAL.docx**):
  * Полное описание Smart Waterfall Engine v12.0, $\alpha=0.3$, авто-сезонности, no-show 97%, бэктестинга и дашборда аналитики.
  * Созданы и встроены свежие скриншоты интерфейса v12.0 (`manual_screenshots/`).
  * Сохранены все оригинальные скриншоты пошаговой выгрузки отчетов из системы Astra DCS (`astra_images/`).
  * Документация переориентирована исключительно на онлайн-доступ `https://boostandgo.ru/aerobag/`.
  * Сформирован полный production-архив `aerobag_build.zip` для загрузки на хостинг.
- Исправлен перенос пассажиров в блок Preliminary:
  * При активном переключателе «Бронь (97% явка)» в Preliminary переносится рассчитанное количество пассажиров с учетом неявки (например, 146 вместо 150).
  * При активном переключателе «Факт (100%)» переносится исходное фактическое количество пассажиров.
- Реализовано мгновенное автосохранение раскладки BULK-отсеков в историю:
  * При расчете и переносе в Preliminary активный рейс связывается с `currentActivePredictionId`.
  * Любое изменение мест, весов или блокировок в BULK 1..12 мгновенно сохраняется в объект рейса в истории (`saveCompartmentsToPrediction()`).
  * При клике на любой рейс из истории в таблице вся раскладка отсеков, веса и блокировки полностью восстанавливаются.
- Внедрена автоматическая подстановка среднего числа пассажиров по рейсу/направлению:
  * При выборе рейса (вводе номера, автокомплите или смене маршрута) поле «Пассажиры (PAX)» автоматически заполняется историческим средним значением по данному рейсу/направлению из базы (например, 207 для рейса 509 AER-TJM, 187 для 505 AER-KZN).
  * Мгновенно пересчитывается подсказка ожидаемой явки (3% No-Show).
- Исправлен расчёт бэктестинга для начальных рейсов базы:
  * Устранён фиктивный нулевой фолбэк (`newPred = actualWeight`), приводивший к нулевым разницам (0 кг) на самых ранних рейсах выборки без накопленной истории.
  * Внедрён честный baseline-расчёт и равномерная выборка 15 репрезентативных рейсов со всех этапов тестового периода.
- Унификация кодов аэропортов по стандарту ИАТА:
  * В `app.js` исправлено сохранение ручных рейсов — запись ведется строго в 3-буквенном формате ИАТА (`from: fromIata, to: toIata`).
  * В функции загрузки (`loadUserFlights`, `loadFlightsFromLocalStorage`) и импорта бэкапов (`handleImportDatabase`) добавлена функция нормализации `normalizeFlightRecord`.
  * Сконвертированы все 129 рейсов с русскими кодами в файле `aerobag_backup_2026_08_20.json` (0 кириллических кодов осталось).
- Режим явки пассажиров по умолчанию переключен на «ФАКТ (100%)».
- Внедрена система аутентификации, разграничения прав (RBAC) и автоматического резервного копирования:
  * Создана таблица `users` с безопасным хешированием паролей (`bcrypt`) и автоинициализацией главного администратора (`admin` / `AeroBag#2026!Master`).
  * Роли: `dispatcher` (доступ только к «Прогнозирование» и «Дашборд») и `admin` (полный доступ ко всем вкладкам + управление пользователями).
  * Во вкладку «Администрирование» добавлены панель управления пользователями (создание, смена паролей, блокировка) и панель автоматических бэкапов.
  * Разработан скрипт `cron_backup.php` для автоматического создания ежедневных бэкапов на сервере с авторотацией за 30 дней и защитой `.htaccess`.
  * Снято ограничение ширины блока имени пользователя в шапке (`.user-fullname`) для полного отображения длинных позывных и ФИО.
  * Исправлена шапка (Header): сгруппирован левый блок (.header-left), предотвращено наплывание текста и бейджей статуса.
  * Во вкладке «Администрирование» панели RBAC и автобэкапов перенесены между ручным вводом и таблицей базы рейсов.
  * Оптимизированы размеры и отступы кнопок шапки для исключения вылезания за пределы контейнера.
  * В подвал (Footer) добавлен копирайт «Разработчик: Andrey Zubkov».
  * Полностью переработано руководство пользователя и администратора (USER_MANUAL.md и USER_MANUAL.docx): разделено на 2 части (Диспетчеры и Администраторы), удалена излишняя математическая терминология, обновлены все скриншоты UI с сохранением шагов Astra DCS.
  * Добавлены кнопки переключения видимости пароля (Глазок 👁️ / 🙈) во всех формах (вход, создание/редактирование пользователя, смена пароля).
  * Добавлена полноценная локальная поддержка (Offline Fallback) для создания, редактирования, смены паролей и удаления пользователей без обращения к серверу.
  * Устранен визуальный баг наплывания статусной плашки на профиль пользователя в шапке (жесткая фиксация flex-shrink: 0, скрытие подзаголовка на разрешениях до 1440px, оптимизация padding).
  * Выполнен аудит безопасности и применены патчи: безопасные флаги сессий (HttpOnly, SameSite=Lax), защита от подбора паролей (Rate limiting / Brute-force delay), запрет прямого HTTP-вызова cron_backup.php, расширенная защита .htaccess (Apache 2.2/2.4) для backups и конфигурационных файлов, создан db_config.example.php.
  * Интегрирован обновленный файл USER_MANUAL.docx от пользователя и пересобран архив aerobag_build.zip для деплоя на хостинг.
  * Разработан новый тематический авиационный фавикон (самолет + чемодан на радаре), созданы favicon.svg, favicon-16x16, favicon-32x32, icon-192, icon-512, многослойные ICO и manifest.json для мгновенной установки как отдельное приложение в Google Chrome.
  * Устранено мелькание окна авторизации при обновлении страницы (добавлен класс hidden по умолчанию в HTML).
  * Реализована автоматическая мгновенная подгрузка базы рейсов при старте приложения и входе пользователя без необходимости ручного нажатия кнопки.
  * Устранено мелькание кнопки вкладки «Администрирование» при перезагрузке страницы для диспетчеров (добавлен класс hidden и display:none по умолчанию в HTML, отображается только после подтверждения роли admin).
  * Внедрен мгновенный синхронный пре-рендер профиля (имя, роль, вкладки) из локального кэша сессии при старте DOM, что полностью исключило мелькание шапки и вкладок для Администраторов и Диспетчеров.
  * Внедрена интерактивная панель управления фильтрами аэропортов и городов для импорта рейсов (Astra DCS / Excel) во вкладке «Администрирование» (размещена между формой добавления рейса и RBAC). Поддерживает IATA, РФ-коды и названия городов на русском, чипсы-теги, синхронизацию с MySQL и режим строгого/нестрогого фильтра прилетов (Вариант А).
  * Восстановлена и усилена 100% автономная локальная/оффлайн работа приложения: встроен справочник DEFAULT_BAGGAGE_DB прямо в код (для file:/// и статических серверов), улучшено автоматическое переключение в оффлайн-режим при недоступности MySQL, обеспечен локальный вход и сохранение сессии.
  * Панель фильтров импорта маршрутов сделана компактно сложенной по умолчанию (аккордеон с бейджами сводки и кнопкой Развернуть/Свернуть), устранено дублирование городов вылета (DYU/ДШБ, KQT/КГТ объединены в единые чипсы), и выполнена строгая сортировка городов по алфавиту (А-Я).
  * Исправлена работа кнопки Развернуть/Свернуть (устранено двойное срабатывание событий), а отображение городов переведено в удобный строчный вид (в столбец с прокруткой), где каждый город занимает отдельную структурированную плашку.
  * Настроен и внедрен CI/CD пайплайн автодеплоя стабильных версий на хостинг Beget через GitHub Actions (.github/workflows/deploy.yml) по пушу в ветку production.




